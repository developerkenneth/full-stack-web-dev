/**
 * | Method                    | Description                                                                       |
| ------------------------- | --------------------------------------------------------------------------------- |
| `readAsText(file)`        | Reads the file and returns its content as text.                                   |
| `readAsDataURL(file)`     | Reads the file and returns a Base64-encoded string (used for images or previews). |
| `readAsArrayBuffer(file)` | Reads the file as an ArrayBuffer (used for binary data).                          |

 */